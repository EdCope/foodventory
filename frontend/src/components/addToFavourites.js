import axios from "axios"
import { useState } from "react"

export const AddToFavourites = (props) => {
  return (
    <button id="add-to-favourites" type="button" className="btn btn-warning">
      Add to Favourites
    </button>
  )
}